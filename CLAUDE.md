# wvb-hub — NCAA D-I Women's College Volleyball personal hub

**Working dir:** `/Users/codyrose/Womens_College_Volleyball_2026` (GitHub repo name: `wvb-hub`).
**You are:** Claude Code = the **Builder** (pure builder on auto mode — build/test, post logs to Drive, do NOT make product decisions or pre-assign roles). Team: **Cody** (Principal/decider), **Gemini** (Architect — Google + math), **Claude app** (Research & Review). Seats share NO memory/session — **Google Drive is the only shared bus.**

## Read first
1. Your **memory** (loaded automatically). Only two memories live at this path: `wvb-hub-drive-bus` (Drive IDs) and `drive-large-inline-content-unreliable`. Four older memories (`wvb-hub-next-session-migration`, `ncaa-volleyball-tool`, `cody-collaboration-workflow`, `cody-volleyball-rating-model`) stayed behind at `~/.claude/projects/-Users-codyrose-Downloads-handoff/memory/` and are **deliberately not migrated** — the Drive handoff + this file supersede them. Don't go looking for them.
2. Drive folder **"Women's College Volleyball 2026"** (`1uOBksR-O3TRPU6Ej84ymei0F4gFPqLe4`).

   **⚠ CURRENT SESSION-CLOSE = "Builder Session-Close 2026-08-10 F (BUILD COMPLETE)"** (`1-ZJUeGbT3KsOgR2wWLsh4pYGmCSnOVfYhcSjRalLfq4`). Read that one. Provenance table for every displayed field: `1FruPLOVJFw5YHGdXHr7xOgkX_PKmc0E-dx8w6zYN_D0`.
   **Superseded, do NOT act on their "next steps":** "Session-Close 2026-08-10 E" (`1zAS7qD_...`); "Session-Close 2026-08-10 D" (`1GWt5RRZ...`); "Session-Close 2026-08-10 C" (`15K4MHf9...`); "Session-Close 2026-08-10 B" (`1-KLPOAd...`); "Session-Close 2026-08-10 (Phase 1 COMPLETE)" (`1_6QZ_o...`); "Session-Close 2026-08-09-2230 (pre-migration handoff)"; "Session-Close 2026-08-09-2345" (its "PUSH NOT LANDED" is wrong — see Git below).

   **⚠ FIVE STALE DOCS SIT IN THE DRIVE ROOT WITH NEUTRAL TITLES.** They contradict current reality and nothing in their names says so, so a memory-less session told to "read the Drive" would take them as current. Do NOT act on any of them:
   - **"Google Services Architecture Research"** (Gemini) — Cloud Run + BigQuery + Connected Sheets + Firebase + Gemini-API extraction. **Every one of those was rejected.** Also contains a fabricated premise (optimising for San Diego, which nobody specified) and claims "sub-millisecond perceived latency", which is impossible over a network.
   - **"Builder - Capabilities & Proposed Build Structure"** — asserts real-browser automation is load-bearing for beating Akamai. **Killed by our own source test:** ncaa.com serves all 348 from a datacenter IP. Its launchd scheduling + staleness watchdog are superseded by `.github/workflows/daily.yml`.
   - **"Research - Architecture Verdict (Aug 9)"** — superseded by that same source test.
   - **"00 - START HERE (Roles, Workflow, Data, The Ask)"** — **most dangerous, because it is titled as the entry point.** Says TCV is the agreed metric (it *lost* the bake-off) and repeats the Akamai claim.
   - **"Volleyball Hub - State and Build-Plan"** and **"Volleyball Hub - Project Brief"** — pre-date everything built since.

   **Session-closes A–E are NOT stale** — they carry `SUPERSEDES` prefixes and are the only record of *why* things are as they are (the Saint Francis diagnosis, the bake-off inverting under opponent adjustment, the append-only dedup analysis, the AQ eligibility-gate bug). Keep them. **Principle: supersede by labelling; delete only things that were never records.**

   **`Data/` and `Specs_for_Builder/` are empty and expected to stay empty.** The day-one folder structure did not survive contact with how the work actually went: datasets live in git (raw is committed, derived is rebuilt on read) and specs arrive as relayed messages. Do not treat them as a convention to follow.

   **AMENDMENT PROTOCOL (revised 2026-08-10 — the earlier version was unexecutable).** The rule "put the correction in the doc it corrects, or have the superseded doc point forward" **cannot be followed**: the Drive connector exposes create/copy/read/download/metadata/search and has **no update or edit operation**, so no existing Drive doc can be modified. Workable version, in force: **this file carries the pointer** (it is read first at boot and is writable), and a superseding doc's title must start with **"SUPERSEDES …"** so the folder listing itself signals it. Never file a bare amendment as a separate doc — it will not be read. Mailbox subfolders: Research/ · Specs_for_Builder/ · Builder_Logs/ · Data/. Write build logs to Builder_Logs/; end each session with a session-close incl. a **LOCAL ONLY** list (what's on disk but not on Drive).

## Rules for the Builder (structural, not vibes)

**R1 — NEVER WRITE AN INTERPRETATION BEFORE THE NUMBER EXISTS.** No output string that *characterizes* a result may be authored before the value it characterizes is computed. Interpretation strings are built from measured values at print time, or they do not exist. Verdicts must come from threshold-free statistics (AUC, permutation test, bootstrap CI) — if a verdict depends on a cutoff you chose yourself, you have tested nothing.
Twice-burned, both in one session:
- Phase 2 printed **"SCATTERED — investigate"** on a *correct* RPI calculation, because I scored a positions-based adjustment with a Pearson correlation against a 0.35 threshold I invented. Fixed with a permutation test: gap +1.92 positions, p=0.00005.
- Phase 3 printed **"lands close to his 2:1"** before the fit ran. It came out **1.00:1** and the sentence printed anyway.
**Why this rule outranks its apparent size:** Cody is the only reviewer and he reads the summaries, not the code. A confidently worded wrong interpretation is the highest-leverage error available to the Builder.

**R2 — COMPLETENESS RULE FOR THE CRAWL. Do not "optimise" this away.**
A **date** is authoritative only when (1) it is strictly in the past *and* (2) every game on it is final. A **game** is authoritative only when `game_state == 'F'`. Everything else stays refetchable.
On a finished season "skip anything already on disk" is correct and fast. On a **live** season it silently destroys data: a date fetched at 3pm caches the afternoon's partial slate as complete and the evening matches are never fetched again — unrecoverable, and it corrupts the game graph everything reconciles against. `games.jsonl` is append-only, so dedup is **final-beats-non-final, then last-wins** (`scripts/gamelog.py`); first-wins dedup would make a stale in-progress record permanently defeat its own correction. Guarded by `scripts/test_crawl_freshness.py` — verified to FAIL against the old behaviour.

**R5 — NEVER SYNTHESISE A DISPLAYED VALUE.** No placeholder, no "illustrative" stand-in, no default that stands where a measurement belongs. Missing data renders as **—** and the coverage is stated on the page. The live site showed a 2026 "returning production %" derived from a **hash of the team name** for ~316 of 348 teams, next to a banner citing *"official 2025 player stats"* — that combination is worse than showing nothing. One cause, three visible symptoms: the 90% cluster was the hash ceiling under a descending sort, "Veteran core" everywhere was `ret>=0.8` at the top of that sort, and New Haven scored 90% with no 2025 stats at all. Hashing a label for **decoration** (a colour) is fine; for a **measurement** it never is. Guarded in `test_display_invariants.py`.

**R4 — CHANGING A FIELD'S MEANING MEANS AUDITING EVERY CONSUMER.** Repointing `pps` from offense-only points/set to opponent-adjusted *net* points/set renamed one of its four call sites; the other three still meant the offensive quantity, and the live page showed **"Ark.-Pine Bluff 2025 Pts/Set −14.31"**. Every number was computed correctly and rendered under a heading that made it wrong. `grep` every consumer before repurposing a field, or give the new meaning a new name. Guarded by `scripts/test_display_invariants.py` (sign/range/consistency on the BUILT artifact — verified to fail against the reintroduced bug: 172 negative + 284 range violations).
**Note what did NOT catch it:** the crawl was correct, reconcile was 348/348, freshness tests passed, CI was green. A human looked at the page. Data-correctness tests cannot catch semantic mislabelling.

**R3 — STRENGTH ≠ RESUME.** Two ratings, two jobs, never merged. The **composite** (strength) answers "who would win a match" and drives the simulator. The **resume view** (RPI, KPI, W-L, record vs top 25/50) answers "who has earned selection" and is what the projector must predict, because the committee weights won-lost results. Measured: relative to RPI the composite favours teams with *worse* records (corr −0.205), so using it to project the field would systematically over-select good-margin/bad-record teams.

## Cody's settled decisions (build against these)
- **History:** every run commits a **timestamped data snapshot to git** (no DB).
- **Schema:** **raw counts, never derived rates** — pull the ncaa.com superset (ids 45–51: kills, attack errors, total attacks, assists, digs, aces, block solos + block assists SEPARATE) + opponent points (box scores) + set scores + a **SOURCE-TIER** field (OFFICIAL/DERIVED/THIRD-PARTY/UNVERIFIED).
- **Architecture:** minimal — **Python compute + git + GitHub Pages.** No Sheets-as-DB / Cloud Run / Firebase / Apps Script. Hosting = phone access only (no auth/domain/polish). GH Pages private repo needs a paid plan → if not paid, make the repo **public** (public stats).
- **Display:** compute all 348, show top-N with a toggle for all.
- **Do NOT build the rating metric yet** — pipeline first, then measure net-points/set vs TCV vs original Adj against 2025 outcomes.

## Measured facts (don't re-derive)
- `ncaa.com` works from a **datacenter IP** (Akamai block is only `stats.ncaa.org`) → daily pipeline can be fully cloud.
- Team per-set stats: `ncaa-api.henrygd.me/stats/volleyball-women/d1/{SEASON}/team/{id}` — **always pin the season; `/current/` is BANNED (see below)**. Paginated 7 pages × 50 = **343** teams (not 348 — see the reclassifier note below). **45 Hitting% (Kills/Errors/Total Attacks), 46 Kills/Set, 47 Assists/Set, 48 Aces/Set, 49 Blocks/Set (Solos+Assists sep), 50 Digs/Set, 51 W-L%.** RPI ranking endpoint = 348 teams but no raw RPI/SOS → derive + reconcile vs official rank ordering + figstats.
- **SOLVED 2026-08-09 — opponent points/set.** Not in `/boxscore`; it's in **`/game/{id}` → `linescores[]`** = `{period, home, visit}` per set (both teams ⇒ points for AND against, per set). `/boxscore` → `teamBoxscore[].teamStats.points` = the **match total** (rally points, NOT the kills+aces+blocks stat formula), and its per-set `sets[]` array is **attack-only** (kills/attackErrors/attackAttempts/hit%) — no scores. **Cross-check: Σ linescores == teamStats.points**, verified on 4 games (3-set, two 5-set incl. 15-13/15-12 deciders, championship 6500718). Game ids via `/scoreboard/volleyball-women/d1/{YYYY}/{MM}/{DD}/all-conf`. Dead endpoints: `gameInfo`/`scoringSummary`/`teamStats`/`linescore` → 422.
- **SOLVED 2026-08-10 — the game log IS enumerable.** `/scoreboard/volleyball-women/d1/{YYYY}/{MM}/{DD}/all-conf`, **no pagination**, one request per date. 139 dates (2025-08-15→12-31) ⇒ **5,131 unique games, season runs 2025-08-22 → 12-21**. `/game/{id}` returns per team `teamId`, `isHome`, **`division`** (the D-I flag RPI Factor I needs), `record`, `score` (sets), plus `linescores[]`. Crawler: `scripts/crawl_2025.py` (resumable, ~1.4 req/s, atomic checkpoints).
- **`/current/` IS BANNED IN THIS CODEBASE — always pin the season.** It resolves to whatever season ncaa.com considers active, so the same URL silently returns different data after a rollover: no error, just 2026 rows where 2025 was expected. **RESOLVED 2026-08-10:** the year segment *is* honored (`/2023/`→Nebraska 33-2, `/2024/`→Penn St. 35-2, `/2025/`→Nebraska 33-1), and a full 49-page regression confirmed **`/2025/` is byte-identical to what `/current/` returned** (`crawl_2025.py verify-pin`, 49/49). `SEASON` constant lives at the top of `crawl_2025.py`. Re-run `verify-pin` any time the pull is repeated.
- **⚠ THE RANKINGS ENDPOINT CANNOT BE SEASON-PINNED.** `/rankings/volleyball-women/d1/ncaa-womens-volleyball-rpi` is **current-only**; every pinned variant 404s. So the official RPI table is unfetchable for any past season. **`data/raw/2025/rpi_official.json` is IRREPLACEABLE** — captured 2026-08-10 while 2025 was still current, and it is also the authority for D-I membership. Never delete it; it cannot be regenerated after Aug 21, 2026.
- **343, not 348, in the stat leaderboards.** Categories 45–51 stop at rank 343. The 5 absent: **Le Moyne, Mercyhurst, Merrimack, New Haven, West Ga.** — championship-ineligible reclassifiers, present in the RPI table (348) but excluded from stat leaderboards. Confirms Research §3 and resolves its open "verify Le Moyne" flag toward *still ineligible*. ⇒ **RPI must be built off the game log, not the leaderboards.**
- **Team NAMES are not a safe join key — use `teamId`.** ncaa.com renders the same school differently across endpoints (RPI "New Orleans" = stats "LSU New Orleans", a 2025 rebrand). `scripts/reconcile_2025.py:norm()` holds the normalizer + alias map.
- **The official RPI `Record` column is DIVISION-I-ONLY**, with non-D-I broken out into `Non-Div I`. Proof from the table itself: Mississippi Val. = `Record 1-17` / `Non-Div I 3-3`, incoherent unless non-D-I is excluded (same at Alcorn, S.C. State). Matches the manual. Only 8 teams have any non-D-I games (20 game-slots league-wide). **Reconcile D-I-only vs `Record`** — comparing total W-L fabricates ~8 false mismatches.
- **TRAP (REFINED 2026-08-10) — `/game/{id}`'s per-team `division` is the team's CURRENT division.** It is unreliable **retroactively**, NOT in general: read *during* the season it describes it is correct (measured on 2026 fixture 6625058 before any play: Norfolk St. div=1, Bowie St. div=2). So: **finished season → archived RPI table; live season → division flags** (`scripts/membership.py`, precedence + source label). Validated: live flags on 2025 give 348 teams overlapping the official 348 by 347, differing by exactly West Florida and Saint Francis — the two schools whose division changed. Original wording below still applies to historical seasons: Saint Francis (PA) played 2025 in D-I (official RPI 20-9) but is now served as `div3`; West Florida is served as `div1` on an all-D-II schedule. Trusting this field left **18 teams short** and produced a 330/348 reconcile. **Take D-I membership from the official RPI table** (the 348) — the only self-consistent flag. Fixed in `reconcile_2025.py` + `build_dataset.py`.
- **RECONCILED 2026-08-10 — the 2025 game graph is COMPLETE. 348/348.** Every official RPI record reproduces exactly from the derived D-I game log; the derived non-D-I split independently matches the official `Non-Div I` column for all 8 teams with such games. 5,131 games, 0 fetch failures, 0 non-final, 0 malformed. **RPI Factors II/III can be trusted to run on this graph.** 34 games in the log have neither team D-I (West Florida's D-II schedule leaking onto the D-I scoreboard) — excluded from all D-I tallies.
- **Python is 3.9.6** (system, Command Line Tools) — **target 3.9**: no `X | Y` unions, no builtin generics (`list[str]`) in annotations, no `match`. Use `typing.List/Dict/Optional/Union`. Decided over upgrading, to avoid a version change mid-build.
- Class year → school roster pages (annual). KPI = proprietary, fetch-only. AQ count/method → config + per-conference data.

## What exists here
`output/vb_dashboard.html` (built dashboard, **40-team sample**, embedded logos) + `vb_template.html` (source; `scripts/build_vb.py` injects data). `data/vb_*.json` = the 40-team datasets (model, rosters, transfers, official RPI, logos). Scale to **all 348** in Phase 1.

## Git
Repo initialized 2026-08-09 on `main`, commit `499a537` (20 files). Identity set **repo-local** (`Cody` / GitHub noreply) — global config untouched. `.gitignore` blocks credentials preventively (there are none: Drive auth is the claude.ai MCP connector's server-side OAuth, not a local `token.json`), plus `.claude/settings.local.json` and `full terminal convo.pdf` (transcript, not a build input — one line to re-include). `gh` authed as `6vrhm29rzs-a11y`.
**Pushed 2026-08-09 ~23:50** — live and public at `https://github.com/6vrhm29rzs-a11y/wvb-hub`, remote `main` = `a240483`.
**Verify remote state with `git ls-remote origin` on boot** — do NOT trust the last written statement about it. (A 2345 session-close said "PUSH NOT LANDED"; the correcting amendment was filed as a *separate* Drive doc and went unread on the next boot. Corrections go in the doc they correct, or the superseded doc carries a pointer.)

## Next steps
**THE BUILD IS COMPLETE (2026-08-10).** Pipeline autonomous · RPI reconciled 348/348 · fitted composite beats RPI out-of-sample · field projector 62/64 · rally simulator calibrated (Brier 0.1289, every bucket within 3.4 pts over 5,014 matches). **Next action: none.** Let it run a week against live data and see what breaks in reality rather than in a dry run.
**Do NOT start without Cody deciding:** KPI scraping (faktorsports), the momentum term (series length under-predicts sweeps 4.6 pts — rally-independence signature, reported not tuned), a conference-bracket data source (the two AQ misses), the Sheets override layer.

**Phases 1-3 detail.** Rating shipped: composite = 1.27·Z(RPI) + 1.27·Z(opponent-adjusted net points/set), weights **fitted** not hand-entered, beats RPI out-of-sample at three cutoffs (+0.025/+0.019/+0.016 AUC, CIs clear of zero). Dashboard runs on all 348 with the vs-RPI delta column. **⚠ It is a STRENGTH rating, not a RESUME rating** — relative to RPI it favours teams with *worse* records (corr −0.205), so it is not automatically right for at-large bracketology. Cody's call.
**✅ The two live-crawl bugs are FIXED and TESTED** (see R2 and `scripts/test_crawl_freshness.py`, verified to fail against the old behaviour). Run that test before any live-season crawl.
**Measured:** the opponent adjustment survives tiny samples (ridge holds the range to ±11.6 at *three* games/team) and its edge over RPI is **largest in September** (+0.064 AUC at 6 games/team, decaying to +0.016 by November) — RPI needs a schedule graph before it means anything, margin does not.

**Phase 1 detail (2026-08-10).** `data/data_2025.json` = 384 teams (348 D-I + 36 non-D-I opponents), 5,131 games, raw counts + linescores, source-tiered, dated. Snapshot in `data/snapshots/`. Raw crawl in `data/raw/2025/`. Scripts: `crawl_2025.py` (resumable), `reconcile_2025.py`, `build_dataset.py`.

**Phase 2 — build RPI Factors I–III** (the spec now exists: Research/ "RPI Spec, Selection Criteria…" = `1VtM8pdKOtz6RazClpYChKt6ozrp5fFeMCQBzXswgo0U`). **25% W% / 50% OW% / 25% OOW%, UNWEIGHTED — no home/road multipliers** (volleyball adopted neither basketball's 0.6/1.4 nor baseball's 0.7/1.3; blogs claiming otherwise are wrong). D-I opponents only; self-exclusion in Factors II/III is INFERRED convention — log it as such. Factor IV is expressed in approximate RPI *positions* and is NOT exactly reproducible — label any overlay DERIVED. Reconcile vs the published rank ordering; divergence concentrated on teams with marquee wins / bad losses is **expected and is evidence the base calc is right**.

**Then the metric bake-off** — still deferred by design: net points/set vs TCV vs original Adj vs hitting-efficiency differential, measured against 2025 outcomes. Claude-app pre-registered its expectation (hitting-eff differential and net pts/set lead) — keep that falsifiable.

**Open / carry forward:** snapshot growth (4.3 MB per run — daily in-season is ~500 MB/season, needs a retention policy); AQ config 32/32 with 31/33 fallback; Big Ten + Pac-12 now award AQ by **tournament** (new for 2026); KPI is read-only from faktorsports.com (THIRD-PARTY tier).
