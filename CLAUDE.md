# wvb-hub — NCAA D-I Women's College Volleyball personal hub

**Working dir:** `/Users/codyrose/Womens_College_Volleyball_2026` (GitHub repo name: `wvb-hub`).
**You are:** Claude Code = the **Builder** (pure builder on auto mode — build/test, post logs to Drive, do NOT make product decisions or pre-assign roles). Team: **Cody** (Principal/decider), **Gemini** (Architect — Google + math), **Claude app** (Research & Review). Seats share NO memory/session — **Google Drive is the only shared bus.**

## Read first
1. Your **memory** (loaded automatically). Four memories live at this path: `wvb-hub-drive-bus` (Drive IDs), `drive-large-inline-content-unreliable`, `git-actions-need-codys-explicit-command`, and `codys-mid-build-observations-are-signal`. Four older memories (`wvb-hub-next-session-migration`, `ncaa-volleyball-tool`, `cody-collaboration-workflow`, `cody-volleyball-rating-model`) stayed behind at `~/.claude/projects/-Users-codyrose-Downloads-handoff/memory/` and are **deliberately not migrated** — the Drive handoff + this file supersede them. Don't go looking for them.
2. Drive folder **"Women's College Volleyball 2026"** (`1uOBksR-O3TRPU6Ej84ymei0F4gFPqLe4`).

   **⚠⚠ RESUME HERE — AND IT IS NOT ON DRIVE. READ `docs/session_close_2026-08-23.md` FIRST** (it supersedes `session_close_2026-08-22.md`, which is still accurate for the churn/sim work).
   It is in this repo, it supersedes every Drive session-close including I, and it is the only
   record of the live season. **The Drive close I (2026-08-11) is now STALE on all three of its
   headline claims** — it says everything is pushed, the tree is clean, and no decision is
   waiting. As of 2026-08-22: **the session's work IS COMMITTED LOCALLY at `a25bb7f` (205 files) but is NOT PUSHED — `origin/main` is still `ec3776f`.** Cody has the push command and has not run it yet; git is classifier-blocked in auto mode, so do NOT attempt it — hand him the command. The commit includes `data/raw/2024/` (48 MB, the 5,186-game backfill) and ~12 new `scripts/` (`build_hub.py`, `availability.py`, `measure_level_effect.py`, `simulate_season_2026.py`, `predict_2026.py`, `venues.py`, …). **Verify real remote state with `git ls-remote origin` on boot rather than trusting this line.** Read close I (`1aHZDME4-1b9ySB6mYw6jXEcDlReEwaEXrzzyTwRj7QU`)
   only for pre-season history. Close G (`1_EBUdhz...`) still holds for roster-parser reasoning
   and the R6/R7 derivation. Provenance table: `1FruPLOVJFw5YHGdXHr7xOgkX_PKmc0E-dx8w6zYN_D0`.
   Do NOT re-run any crawl, including `crawl_aq.py`. The 88.0% join question is CLOSED (it was
   never a defect rate; measured defect rate 0.09%).

   **Superseded, do NOT act on their "next steps":** "Session-Close 2026-08-10 H" (`1KMqQ__jr...` — its RESUME block is spent; read it only for roster-parser history); "Session-Close 2026-08-10 F" (`1-ZJUeGb...`); "Session-Close 2026-08-10 E" (`1zAS7qD_...`); "Session-Close 2026-08-10 D" (`1GWt5RRZ...`); "Session-Close 2026-08-10 C" (`15K4MHf9...`); "Session-Close 2026-08-10 B" (`1-KLPOAd...`); "Session-Close 2026-08-10 (Phase 1 COMPLETE)" (`1_6QZ_o...`); "Session-Close 2026-08-09-2230 (pre-migration handoff)"; "Session-Close 2026-08-09-2345" (its "PUSH NOT LANDED" is wrong — see Git below).

   **⚠ FIVE STALE DOCS SIT IN THE DRIVE ROOT WITH NEUTRAL TITLES.** They contradict current reality and nothing in their names says so, so a memory-less session told to "read the Drive" would take them as current. Do NOT act on any of them:
   - **"Google Services Architecture Research"** (Gemini) — Cloud Run + BigQuery + Connected Sheets + Firebase + Gemini-API extraction. **Every one of those was rejected.** Also contains a fabricated premise (optimising for San Diego, which nobody specified) and claims "sub-millisecond perceived latency", which is impossible over a network.
   - **"Builder - Capabilities & Proposed Build Structure"** — asserts real-browser automation is load-bearing for beating Akamai. **Killed by our own source test:** ncaa.com serves all 348 from a datacenter IP. Its launchd scheduling + staleness watchdog are superseded by `.github/workflows/daily.yml`.
   - **"Research - Architecture Verdict (Aug 9)"** — superseded by that same source test.
   - **"00 - START HERE (Roles, Workflow, Data, The Ask)"** — **most dangerous, because it is titled as the entry point.** Says TCV is the agreed metric (it *lost* the bake-off) and repeats the Akamai claim.
   - **"Volleyball Hub - State and Build-Plan"** and **"Volleyball Hub - Project Brief"** — pre-date everything built since.

   **Session-closes A–E are NOT stale** — they carry `SUPERSEDES` prefixes and are the only record of *why* things are as they are (the Saint Francis diagnosis, the bake-off inverting under opponent adjustment, the append-only dedup analysis, the AQ eligibility-gate bug). Keep them. **Principle: supersede by labelling; delete only things that were never records.**

   **`Data/` and `Specs_for_Builder/` are empty and expected to stay empty.** The day-one folder structure did not survive contact with how the work actually went: datasets live in git (raw is committed, derived is rebuilt on read) and specs arrive as relayed messages. Do not treat them as a convention to follow.

   **⚑ RUN AUTONOMOUSLY (Cody, 2026-08-10).** Work the agreed queue end to end; settled decisions stay settled. **Do not surface half-built work** — he was asked to check a phone layout five times across four failed fixes, which is the wrong use of the principal. Surface only for: something genuinely shippable, something a seat physically cannot do, a decision only he can make, or a finding that changes the plan — and **batch** those into one list. Builder cannot: `git push`, Drive delete/rename/move, anything on a physical phone, account/billing.

   **👤 `For_Cody/` = `1sOiNGxcDPMDQB9dYMNGpemgvJkM5Brhj` — the human-readable summary layer.** Exec summary first, bullets not paragraphs, plain English, numbered so reading order is obvious, actions under their own heading. **It is a summary layer, NOT the record** — depth stays in `Builder_Logs/` and `Research/`; summarise and point.

   **📓 `Cody_Log/` = `1CHqDTgoFlUB8h-gQp0ldtWyVVCILgcoX` — READ AT SESSION START.** Cody speaks to each seat separately and **neither seat sees what he told the other**; his instructions were the only artifact with no durable record. Both seats append `CODY SAID — to <SEAT>, <DATE> (<topic>)` capturing substance and standing instructions. `SUPERSEDES` in the title if he reverses something.

   **📬 SEAT-TO-SEAT MAILBOXES (adopted 2026-08-10).** Cody's copy-paste is unreliable, so seats exchange messages as **Drive files**, and he just says *"check the drive"*. Do not ask him to relay text.
   - `Specs_for_Builder/` = `1otrJ6BCyN8aLZeCJWKIbtD2atGi4pkds` — **Claude-app → Builder.** Check it when told to.
   - `Messages_to_Claude_App/` = `1njWwkE3Yotd7H-k5XsE8gCq_zq-wkn-1` — **Builder → Claude-app.** Write replies and handoffs here.
   - `Builder_Logs/` = session-closes and build logs. `Research/` = Claude-app's records.
   **Convention:** title starts `MESSAGE TO <SEAT>` + date; add `SUPERSEDES` if it replaces an earlier one. Neither seat can delete or rename in Drive (create/copy/read/download/metadata/search only), so **labelling is the only mechanism either of us has** — the same constraint that produced the unread-amendment failure.

   **AMENDMENT PROTOCOL (revised 2026-08-10 — the earlier version was unexecutable).** The rule "put the correction in the doc it corrects, or have the superseded doc point forward" **cannot be followed**: the Drive connector exposes create/copy/read/download/metadata/search and has **no update or edit operation**, so no existing Drive doc can be modified. Workable version, in force: **this file carries the pointer** (it is read first at boot and is writable), and a superseding doc's title must start with **"SUPERSEDES …"** so the folder listing itself signals it. Never file a bare amendment as a separate doc — it will not be read. Mailbox subfolders: Research/ · Specs_for_Builder/ · Builder_Logs/ · Data/. Write build logs to Builder_Logs/; end each session with a session-close incl. a **LOCAL ONLY** list (what's on disk but not on Drive).

## Rules for the Builder (structural, not vibes)

**R1 — NEVER WRITE AN INTERPRETATION BEFORE THE NUMBER EXISTS.** No output string that *characterizes* a result may be authored before the value it characterizes is computed. Interpretation strings are built from measured values at print time, or they do not exist. Verdicts must come from threshold-free statistics (AUC, permutation test, bootstrap CI) — if a verdict depends on a cutoff you chose yourself, you have tested nothing.
Twice-burned, both in one session:
- Phase 2 printed **"SCATTERED — investigate"** on a *correct* RPI calculation, because I scored a positions-based adjustment with a Pearson correlation against a 0.35 threshold I invented. Fixed with a permutation test: gap +1.92 positions, p=0.00005.
- Phase 3 printed **"lands close to his 2:1"** before the fit ran. It came out **1.00:1** and the sentence printed anyway.
**Why this rule outranks its apparent size:** Cody is the only reviewer and he reads the summaries, not the code. A confidently worded wrong interpretation is the highest-leverage error available to the Builder.

**R2 — COMPLETENESS RULE FOR THE CRAWL. Do not "optimise" this away.**
A **date** is authoritative only when (1) it is strictly in the past *and* (2) every game on it is final. A **game** is authoritative only when `game_state == 'F'`. Everything else stays refetchable.
On a finished season "skip anything already on disk" is correct and fast. On a **live** season it silently destroys data: a date fetched at 3pm caches the afternoon's partial slate as complete and the evening matches are never fetched again — unrecoverable, and it corrupts the game graph everything reconciles against. `games.jsonl` is append-only, so dedup is **final-beats-non-final, then last-wins** (`scripts/gamelog.py`); first-wins dedup would make a stale in-progress record permanently defeat its own correction. Guarded by `scripts/test_crawl_freshness.py` — verified to FAIL against the old behaviour.

**R7 — INDEPENDENCE BEATS DEPTH. Do not buy your way out of errors with effort.** Claude-app's evidence review (Builder_Logs / For_Cody, 2026-08-10) settles the question this project kept running into, and the literature matches our own 4-for-4 tally exactly:
- **Self-review adds ~nothing.** Cross-Model review measured Claude self-review at **91.4% → 91.4%** — 3 fixes, 3 regressions, +72% cost. Cross-Context review found the gain comes from **context separation, not extra passes**: repeated self-review did *not* beat single self-review (p=0.11), while a fresh no-context session did (F1 28.6 vs 24.6, p=0.008).
- **More reasoning does not question a false premise — it amplifies it.** No frontier model corrects >43% of false presuppositions; missing-premise questions produce 2–4× longer output that still misses the flaw. That is Saint Francis exactly.
- **Reasoning can crowd out verification:** *"the model spends too long reasoning internally, it begins to form assumptions about the environment rather than verifying them."* All three measurement bugs here were "didn't check" bugs.
- **Calibration can degrade with reasoning for the Claude family specifically** (Claude 3.7 Sonnet got *worse* calibrated with more reasoning).
**Operationally:** medium/high effort for mechanical crawling and transformation; reserve max for genuine design/analysis, and always pair it with an independent review because the producing agent rationalises its own frame. **Spend budget on mechanical invariants and a fresh-context reviewer, not on thinking harder.** Give the reviewer tools the builder didn't use. Treat any model's advertised "self-verification" as the exact signal this project distrusts.

**R6 — HOW TO VERIFY MOBILE CSS WITHOUT A PHONE.** `resize_window` in Claude-in-Chrome reports success but does NOT change the rendering viewport — `window.innerWidth` stays ~1512 and `matchMedia('(max-width:560px)')` never matches. Treating that as "mobile is unverifiable" cost five review cycles of Cody's time on four failed fixes. **Instead: lift the media block and inspect programmatically** via `javascript_tool` — walk `document.styleSheets`, find `CSSRule.MEDIA_RULE` whose `conditionText` matches, re-inject its `cssRules` as an unconditional `<style>`, then assert on the result (`getComputedStyle(tr).display === 'flex'`, visible cell count, `tr.scrollWidth > width` for overflow, distinct `getBoundingClientRect().top` values to prove wrapping). Verified working: the card layout applies, five cells render, labels stay bound to values, and `overflowsItsBox` is false.

**R5 — NEVER SYNTHESISE A DISPLAYED VALUE.** No placeholder, no "illustrative" stand-in, no default that stands where a measurement belongs. Missing data renders as **—** and the coverage is stated on the page. The live site showed a 2026 "returning production %" derived from a **hash of the team name** for ~316 of 348 teams, next to a banner citing *"official 2025 player stats"* — that combination is worse than showing nothing. One cause, three visible symptoms: the 90% cluster was the hash ceiling under a descending sort, "Veteran core" everywhere was `ret>=0.8` at the top of that sort, and New Haven scored 90% with no 2025 stats at all. Hashing a label for **decoration** (a colour) is fine; for a **measurement** it never is. Guarded in `test_display_invariants.py`.

**R4 — CHANGING A FIELD'S MEANING MEANS AUDITING EVERY CONSUMER.** Repointing `pps` from offense-only points/set to opponent-adjusted *net* points/set renamed one of its four call sites; the other three still meant the offensive quantity, and the live page showed **"Ark.-Pine Bluff 2025 Pts/Set −14.31"**. Every number was computed correctly and rendered under a heading that made it wrong. `grep` every consumer before repurposing a field, or give the new meaning a new name. Guarded by `scripts/test_display_invariants.py` (sign/range/consistency on the BUILT artifact — verified to fail against the reintroduced bug: 172 negative + 284 range violations).
**Note what did NOT catch it:** the crawl was correct, reconcile was 348/348, freshness tests passed, CI was green. A human looked at the page. Data-correctness tests cannot catch semantic mislabelling.

**R8 — A NAME MATCH MUST ANCHOR ON THE SURNAME, AND CONFIRMING EVIDENCE MUST COME FROM OUTSIDE THE DATA IT ADJUDICATES.** Joining 2026 rosters to 2025 production is the one place where a wrong answer *looks* right: every number computed correctly, attributed to the wrong person. Two rules, both paid for:
- **Anchor on an EXACT surname token; let only the given name flex, never the reverse.** The 59 near-name candidates split cleanly — 54 at surname ratio 1.000, four at 0.20–0.60. The four are `Lauren Pyle → Lauren Malone` (316 kills, two different people). A plain difflib join over the whole name makes that mistake.
- **A shared first initial is NOT evidence.** It was silently carrying 25 of 60 joins. Claude-app's review named the hole the uniqueness guard cannot see: one sister in the 2025 pool, the other on the 2026 roster — only one candidate exists, so uniqueness passes and she absorbs her sister's season. Replaced by a **published** diminutive list (`data/raw/nicknames_source.csv` → `data/nicknames.json`), which may only **confirm**: a pair it does not cover stays unresolved and renders **—**. That dropped 11 joins, correctly. **Never hand-add a pair to close a case in front of you** — a map authored from the cases it adjudicates confirms exactly what it was built to confirm, which is the n=2 threshold problem at larger n. (The list's gaps are real: it lacks Izzy/Isabella, Gabi/Gabrielle, Allie/Alexandra. Swapping in a better-covering *published* list is the only sanctioned fix.)
Guarded by `scripts/test_join_invariants.py` — 9 checks plus a **negative control** that re-loosens the surname anchor in-process and asserts the guards trip. The first negative control did not trip; that was the control being wrong, not the guard. **A test that cannot fail is not a test.**

**R3 — STRENGTH ≠ RESUME.** Two ratings, two jobs, never merged. The **composite** (strength) answers "who would win a match" and drives the simulator. The **resume view** (RPI, KPI, W-L, record vs top 25/50) answers "who has earned selection" and is what the projector must predict, because the committee weights won-lost results. Measured: relative to RPI the composite favours teams with *worse* records (corr −0.205), so using it to project the field would systematically over-select good-margin/bad-record teams.

## Cody's settled decisions (build against these)
- **History:** every run commits a **timestamped data snapshot to git** (no DB).
- **Schema:** **raw counts, never derived rates** — pull the ncaa.com superset (ids 45–51: kills, attack errors, total attacks, assists, digs, aces, block solos + block assists SEPARATE) + opponent points (box scores) + set scores + a **SOURCE-TIER** field (OFFICIAL/DERIVED/THIRD-PARTY/UNVERIFIED).
- **Architecture:** minimal — **Python compute + git + GitHub Pages.** No Sheets-as-DB / Cloud Run / Firebase / Apps Script. Hosting = phone access only (no auth/domain/polish). GH Pages private repo needs a paid plan → if not paid, make the repo **public** (public stats).
- **Display:** compute all 348, show top-N with a toggle for all.
- **Do NOT build the rating metric yet** — pipeline first, then measure net-points/set vs TCV vs original Adj against 2025 outcomes.

## Measured facts (don't re-derive)
- `ncaa.com` works from a **datacenter IP** (Akamai block is only `stats.ncaa.org`) → daily pipeline can be fully cloud.
- Team per-set stats: `ncaa-api.henrygd.me/stats/volleyball-women/d1/{SEASON}/team/{id}` — **always pin the season; `/current/` is BANNED (see below)**. Paginated 7 pages × 50 = **343** teams (not 348 — see the reclassifier note below). **45 Hitting% (Kills/Errors/Total Attacks), 46 Kills/Set, 47 Assists/Set, 48 Aces/Set, 49 Blocks/Set (Solos+Assists sep), 50 Digs/Set, 51 W-L%.** RPI ranking endpoint = 348 teams but no raw RPI/SOS → derive + reconcile vs official rank ordering + figstats.
- **SOLVED 2026-08-09 — opponent points/set.** Not in `/boxscore`; it's in **`/game/{id}` → `linescores[]`** = `{period, home, visit}` per set (both teams ⇒ points for AND against, per set). `/boxscore` → `teamBoxscore[].teamStats.points` = the **match total** (rally points, NOT the kills+aces+blocks stat formula), and its per-set `sets[]` array is **attack-only** (kills/attackErrors/attackAttempts/hit%) — no scores. **Cross-check: Σ linescores == teamStats.points**, verified on 4 games (3-set, two 5-set incl. 15-13/15-12 deciders, championship 6500718). Game ids via `/scoreboard/volleyball-women/d1/{YYYY}/{MM}/{DD}/all-conf`. Dead endpoints: `gameInfo`/`scoringSummary`/`teamStats`/`linescore` → 422.
- **SOLVED 2026-08-10 — the game log IS enumerable.** `/scoreboard/volleyball-women/d1/{YYYY}/{MM}/{DD}/all-conf`, **no pagination**, one request per date. 139 dates (2025-08-15→12-31) ⇒ **5,131 unique games, season runs 2025-08-22 → 12-21**. `/game/{id}` returns per team `teamId`, `isHome`, **`division`** (the D-I flag RPI Factor I needs), `record`, `score` (sets), plus `linescores[]`. Crawler: `scripts/crawl_2025.py` (resumable, ~1.4 req/s, atomic checkpoints).
- **`/current/` IS BANNED IN THIS CODEBASE — always pin the season.** It resolves to whatever season ncaa.com considers active, so the same URL silently returns different data after a rollover: no error, just 2026 rows where 2025 was expected. **RESOLVED 2026-08-10:** the year segment *is* honored (`/2023/`→Nebraska 33-2, `/2024/`→Penn St. 35-2, `/2025/`→Nebraska 33-1), and a full 49-page regression confirmed **`/2025/` is byte-identical to what `/current/` returned** (`crawl_2025.py verify-pin`, 49/49). `SEASON` constant lives at the top of `crawl_2025.py`. Re-run `verify-pin` any time the pull is repeated.
- **⚠ THE RANKINGS ENDPOINT CANNOT BE SEASON-PINNED.** `/rankings/volleyball-women/d1/ncaa-womens-volleyball-rpi` is **current-only**; every pinned variant 404s. So the official RPI table is unfetchable for any past season. **`data/raw/2025/rpi_official.json` is IRREPLACEABLE** — captured 2026-08-10 while 2025 was still current, and it is also the authority for D-I membership. Never delete it; it cannot be regenerated after Aug 21, 2026.
- **343, not 348, in the stat leaderboards.** Categories 45–51 stop at rank 343. The 5 absent: **Le Moyne, Mercyhurst, Merrimack, New Haven, West Ga.** — championship-ineligible reclassifiers, present in the RPI table (348) but excluded from stat leaderboards. Confirms Research §3 and resolves its open "verify Le Moyne" flag toward *still ineligible*. ⇒ **RPI must be built off the game log, not the leaderboards.**
- **Team NAMES are not a safe join key — use `teamId`.** ncaa.com renders the same school differently across endpoints (RPI "New Orleans" = stats "LSU New Orleans", a 2025 rebrand). `scripts/reconcile_2025.py:norm()` holds the normalizer + alias map.
- **The official RPI `Record` column is DIVISION-I-ONLY**, with non-D-I broken out into `Non-Div I`. Proof from the table itself: Mississippi Val. = `Record 1-17` / `Non-Div I 3-3`, incoherent unless non-D-I is excluded (same at Alcorn, S.C. State). Matches the manual. Only 8 teams have any non-D-I games (20 game-slots league-wide). **Reconcile D-I-only vs `Record`** — comparing total W-L fabricates ~8 false mismatches.
- **TRAP (REFINED 2026-08-10) — `/game/{id}`'s per-team `division` is the team's CURRENT division.** It is unreliable **retroactively**, NOT in general: read *during* the season it describes it is correct (measured on 2026 fixture 6625058 before any play: Norfolk St. div=1, Bowie St. div=2). So: **finished season → archived RPI table; live season → division flags** (`scripts/membership.py`, precedence + source label). Validated: live flags on 2025 give 348 teams overlapping the official 348 by 347, differing by exactly West Florida and Saint Francis — the two schools whose division changed. Original wording below still applies to historical seasons: Saint Francis (PA) played 2025 in D-I (official RPI 20-9) but is now served as `div3`; West Florida is served as `div1` on an all-D-II schedule. Trusting this field left **18 teams short** and produced a 330/348 reconcile. **Take D-I membership from the official RPI table** (the 348) — the only self-consistent flag. Fixed in `reconcile_2025.py` + `build_dataset.py`.
- **RECONCILED 2026-08-10 — the 2025 game graph is COMPLETE. 348/348.** Every official RPI record reproduces exactly from the derived D-I game log; the derived non-D-I split independently matches the official `Non-Div I` column for all 8 teams with such games. 5,131 games, 0 fetch failures, 0 non-final, 0 malformed. **RPI Factors II/III can be trusted to run on this graph.** 34 games in the log have neither team D-I (West Florida's D-II schedule leaking onto the D-I scoreboard) — excluded from all D-I tallies.
- **Python is 3.9.6** (system, Command Line Tools) — **target 3.9**: no `X | Y` unions, no builtin generics (`list[str]`) in annotations, no `match`. Use `typing.List/Dict/Optional/Union`. Decided over upgrading, to avoid a version change mid-build.
- Class year → school roster pages (annual). KPI = proprietary, fetch-only. AQ count/method → config + per-conference data.
- **SOURCE ACCESS IS MEASURED AND WRITTEN DOWN: `docs/data_sources.md` (2026-08-11).** Every source tested by fetching it, with the exact re-check command. Headlines: **`ncaa.com/rankings/volleyball-women/d1` IS the AVCA poll** (parses to rank/school/points with first-place votes, stamped "Through Games") — so the poll needs no new dependency, though like all rankings endpoints it is current-only and history must come from the AVCA `.xlsx` archive. **VolleyballMag is the only blocked domain** (Cloudflare 403 on every HTML page; its RSS works but carries no NCAA-women content). AVCA, VolleyTalk and ncaa.com brackets are all open. **VolleyTalk saved page copies are redundant — the live fetch is byte-identical to a logged-in save.** Don't re-derive any of this; re-run the one-line curl in the doc.
- **⚠ 2024 AND 2025 BOX SCORES DO NOT LIST THE SAME PEOPLE (measured 2026-08-22).** From near-identical game counts (5,186 vs 5,131) the feed returns **170,275 player rows for 2024 against 117,270 for 2025** — 33 per match vs 23. The aggregates land at 9,700 vs 6,017 players, and the tail is the difference: **33% of 2024 players finish with zero kills** against 18% in 2025, with p25 sets of 4 vs 23. Older seasons appear to mark bench players as having participated; 2025 does not. **Consequences:** any "did this player appear last season" test is over-inclusive for 2024, so the **22.3% median newcomer share below is a LOWER bound**; and every cross-season fit must keep its `MIN_SETS` filter, which is what stops the marginal tail from contaminating it. Do not compare raw player counts between seasons and conclude anything about roster sizes.
- **FRESHMAN CONTRIBUTION IS MEASURED — AND STILL NOT MODELLED (2026-08-22).** Share of a team's 2025 production from players with no 2024 D-I record: **median 22.3%, mean 24.4%** (p10 4.4%, p90 47.8%, n=348). Hampton 93.7%, Saint Peter's 87.8%, Vanderbilt 72.6%. The earlier **53.8% figure was wrong** — it dropped ambiguous names from the prior-season universe, so any 2025 player with a common name looked like a newcomer; a name appearing twice in 2024 still means *someone* by that name played. **W_FRESHMAN stays 0 deliberately.** Knowing newcomers supply ~22% does not say how to value a *specific* unknown player, and a flat credit would simply reward teams for carrying more unknowns. What the number buys is honesty: the size of the bias is now stated rather than merely admitted.
- **THE ROSTER TERM IS FITTED AT 0.09, AND THE PRIOR IS THE FOUNDATION (2026-08-22).** The 2024 player backfill completed (5,186 games, 0 failures, 9,700 players), making the honest test possible: build the same prior-plus-roster-delta projection for 2024→2025 and score it **out of sample**, 5-fold, n=323. **Prior alone rho 0.8245 · prior + roster delta 0.8317 · gain +0.0072**, with the delta fitted at **~9% of the prior's weight**. That is why every hand-set value failed: 0.15–1.00 were all far too aggressive and each made the ordering worse, while 0.0 threw away a real effect (partial correlation of the roster with the outcome, prior removed from both sides: **+0.233**). **The roster aggregation NEVER beats the prior alone** — best fitted variant rho 0.806 against 0.827. It is a correction, not a foundation. Also measured: the transfer experiment gives a level effect of **−0.53 pts/set per SD for movers against −0.19 for stayers** (net ≈ −0.34), same sign and order as the within-player estimate of −0.215 from a completely different design. **⚠ The "new-player share" of 53.8% in `fit_2024_2025.json` is NOT trustworthy** — name-only keying drops ambiguous names, inflating apparent newcomers; do not use it as a freshman-contribution estimate.
- **PLAYER HEADSHOTS: URLS ONLY, NEVER THE FILES (2026-08-22).** `crawl_rosters.py` now captures each player's photo URL. **The image is never downloaded and never committed** — this repo is PUBLIC, the photos belong to the schools, and storing a reference is a different act from republishing the file. Two extraction paths were needed because templates differ: an `<img>` near the player's own anchor (SIDEARM), and `itemprop="name"`/`itemprop="image"` schema.org pairs (WMT/Kentucky, which has no `<img>` near the link at all). Two repairs, both measured: WMT emits a **doubled prefix** (`site.com/https://site.com/imgproxy/...`) that 404s, and SIDEARM's crop URLs carry `&amp;` query separators that 400 unless HTML-decoded. **Coverage is ~50%** — templates like UCLA's ship only a base64 1×1 placeholder and load the real URL by JavaScript, so those photos are genuinely absent from the HTML; a player without one renders **initials**, never a stand-in image. imgproxy URLs are signed, so the 1024px size cannot be rewritten smaller.
- **THE SEASON SIMULATOR IS BACKTESTED, AND ITS BAND IS SLIGHTLY WIDE (2026-08-22).** `scripts/simulate_season_2026.py` plays every remaining fixture 4,000 times through the calibrated rally model. Two corrections, both measured 2024→2025 (n=346): the prior is **shrunk by 0.860** (last season overstates how extreme a team will be), and each iteration **redraws every team's strength** from a residual SD of **2.12 pts/set** against a between-team SD of 4.48 — fixing strength makes win bands far too narrow, which is the failure that looks impressive and is wrong. **Backtest: 2024 prior → simulated 2025 → the 80% win-total band contained the truth for 87.3% of teams**, median projection off by 3.74 wins. Left alone rather than tuned to hit 80% exactly on one season. **Projected wins is NOT a quality ranking** — schedule drives it, so Fairfield (MAAC) projects more wins than Texas (SEC). Same strength-vs-resume distinction as R3.
- **AVAILABILITY IS MEASURABLE, INJURY IS NOT (built 2026-08-22).** `scripts/availability.py` flags a match where a team's **top-6 by scoring rate** has no box-score line — "absent", never "injured", because the reason is never in the data. Two calibrations, both paid for: expectation is judged against a **local window** of 4 matches either side, not the season (a season-wide rule reported mid-season roster arrivals as absences and flagged 52% of all team-matches); and only the **top-6 rotation** counts, because a squad carries 14 and resting a reserve is not news. Final rate: **23% of 2025 team-matches** had a top-6 player out — e.g. Nebraska once missing Harper Murray, Andi Jackson and Taylor Landfair together, 10.04 pts/set off the floor. Runs daily for 2026.
- **⚠ PRIOR-SEASON RATES CARRY HEALTH CONTEXT THE BOX SCORE DOES NOT (Cody, 2026-08-22).** Brooklyn DeLeye played the whole of 2025 on a torn meniscus suffered in the Nebraska game, had surgery in the off-season, and is back. Her 2025 points/set therefore UNDERSTATES her, and any projection that treats a prior-year rate as a clean estimate of ability under-rates Kentucky by exactly that much. The same error runs the other way for a player who got hurt late. **Nothing in the feed exposes this** — it is forum and beat-reporter knowledge. Two consequences: (1) treat prior-year rates as a floor-ish estimate with unknown error, not a measurement of ability; (2) the closest thing we can measure is AVAILABILITY — a top-6 player logging 0 sets is visible in the per-game box, which is why 2026 player lines are now collected daily. Do not invent an injury adjustment; if Cody supplies context for a specific player, record it as an explicit, sourced override rather than inferring one.
- **VENUE IS IN THE FEED AND WE NOW STORE IT (2026-08-22).** `/game/{id}` carries `location: {venue, city, stateUsps}`. We were discarding it, so the dashboard inferred "at <home team>" — and was wrong the first weekend it mattered: Kentucky-Wisconsin and Louisville-Texas A&M were both **AVCA First Serve at Fiserv Forum, Milwaukee** — a NEUTRAL floor with a nominal home team. An inferred venue presented as fact is an R5 violation; venue now renders from the feed or says "venue not reported". **This also matters to the rating**, which fits a home-advantage term (`fit_off_def`) and would credit an edge nobody had. Neutral-site detection is NOT yet implemented — the mechanism to build is a team's modal venue across its own games, which self-populates as the season runs.
- **LIVE SCORING IS AVAILABLE AND USABLE (measured 2026-08-22).** A match in progress returns `gameState:"I"`, `currentPeriod:"1ST SET"`, and **`linescores` that update point by point** (Louisville 18, Wisconsin 24 mid-set), plus live records. `scripts/live_server.py` serves the hub and polls once per 60s — **ncaa-api sends no `Access-Control-Allow-Origin`, so a page cannot fetch it directly**; a local process is the only route, and it also keeps N tabs at one upstream request per cycle.
- **⚠ NCAA.COM DELETES GAMES FROM A PAST DATE (measured 2026-08-22).** We crawled **12** games for 2026-08-21 on the 18th; the live scoreboard for that date now returns **2** — Louisville 3-1 Texas A&M and Kentucky 3-0 Wisconsin, the two real AVCA First Serve matches. The other ten were **pulled**: not postponed, not played, gone. Their non-final records sit in the append-only log and **can never resolve**, because the game is no longer enumerated and will never be refetched. That is fine — the log is append-only on purpose — but it breaks the natural assumption that a scheduled game either goes final or stays pending. **Everything downstream must filter `game_state == 'F'`** (`build_dataset.py:137` does). Guarded by `test_crawl_freshness.py` with a negative control. Do NOT add logic that deletes them.
- **⚠ THE PLAYER `points` COLUMN IN THE BOX SCORE IS UNUSABLE AS A SEASON TOTAL (measured 2026-08-11).** Across 4,601 players with >20 sets it is **never above** kills + aces + solo blocks + ½ block assists, and **below it for 3,270 of them** (median ratio 0.61). Rita Benidio reads 155 kills and 106 "points". Signature of a column the feed carries for only *some* games, so the season sum undercounts by a different amount per player. **Derive production from raw counts** (`join_players.py:production()`) — which is what Cody's raw-counts schema decision already required. No other consumer existed (grep), so nothing shipped was corrupted.
- **JERSEY NUMBERS DO NOT CORROBORATE A NAME MATCH (measured 2026-08-11).** Roster number and box-score number are known on both sides for only **6 of 60** surname-anchored joins, and agree on **0 of those 6**. They are not the same field. This will look like an obvious way to confirm an uncertain join again in a few weeks; it is not one.
- **RESOLVED 2026-08-11 — the 88.0% join rate was NOT a defect rate.** Close H's pre-registered edit-distance check (difflib, cutoff 0.72, each unresolved name vs that team's OWN pool) on all 673: **59 had a near name** (candidate defects), **614 had none** (no D-I production to attribute — walk-on, D-II/JUCO/international, never played). Reproduces the 10-school hand-verified result at scale. **Measured defect rate after fixes: 0.09%** (5 of 5,596). `scripts/audit_unresolved.py`, output `data/unresolved_audit.json`.

- **CHURN IS A REAL, PROSPECTIVE PREDICTOR — AND NOT FOR THE REASON POLLSTERS THINK (measured 2026-08-18/22).** Cody's observation was that early polls mark down heavy-turnover teams (SMU) because they "haven't meshed yet." Tested two ways:
  - *Retrospective* (does the penalty fade as a season goes on?) — **it does the opposite.** High-churn quartile residual win%: **−0.019 early → −0.060 late.** So it is NOT a temporary chemistry cost that teams grow out of. It is durable talent loss. The pollsters' instinct is right; their stated reason is wrong.
  - *Prospective* (share of last season's production that did not return to D-I anywhere — knowable the day rosters publish) — this is the usable feature. Fitted **jointly** with the roster delta because both partly measure production lost; stacking separately-fitted weights would double-count it. 2024→2025, 5-fold OOS, n=323: prior **0.8253** · +delta **0.8312** · +churn **0.8387** · +both **0.8405**. z-weights: prior +2.178 / delta +0.123 / churn **−0.284** ⇒ `ROSTER_DELTA_WEIGHT=0.0565`, `CHURN_WEIGHT=−0.1304`. **Churn is worth ~2× the roster delta.** Live in `project_2026.py` (applied as a second pass, z-scored across the field, after every team's share is known; a team with no returning share gets NO adjustment rather than the mean). 320/348 teams adjusted.

- **⚠ AN UNANNOUNCED START TIME ARRIVES AS A REAL-LOOKING 1:00 AM ET, NOT AS A BLANK (measured 2026-08-22).** ncaa.com fills a fixture whose time is not yet set with a midnight-ish sentinel that formats **exactly like an announced time** — `12:00`–`3:00 AM ET` — so it renders authoritatively and reads as fact. Proof it is a placeholder: in the **completed** 2025 season only **13 of 5,133** fixtures carried an early-AM time and **all thirteen were at Hawaii** (1:00 AM ET = 7:00 PM HST, an ordinary evening start in Honolulu); in the 2026 schedule **192** do — 16 at Hawaii and **176 at schools like Nebraska, Alabama and Wisconsin**, which do not host at 1 AM. They are replaced by real times as the date approaches. The feed already has its own representation for an unknown start (`TBA`, on 83 fixtures), so `scripts/build_hub.py:listed_time()` maps the sentinel to that and **keeps Hawaii's genuine late times** (`FAR_WEST_HOME` — the only D-I programme far enough west for this). Both consumers were audited per R4 (`schedule()` and `team_index`); `tv()` reads a manual file and is unaffected. Guarded in `test_display_invariants.py` with a negative control in both directions (sentinel suppressed, Hawaii kept, evening times untouched). **Do not "fix" this by suppressing all early-AM times — that deletes Hawaii's real schedule.**
- **ROTATIONS ARE NOT IN THE FEED — THE ORDER IS JERSEY NUMBER. QUESTION CLOSED (measured 2026-08-22, `docs/rotations_finding.md`).** Endpoint is **`/game/{id}/play-by-play`** (HTTP 200, ~58 KB/match; `pbp` and `playbyplay` both 422). **Cody's ask for "rotations 1-6 vs opponent rotations 1-6" cannot be built from this source. Do not re-attempt it without new evidence; do not infer one from box-score totals (R5).** Four findings, and note that **two of the three claims in the previous version of this bullet were WRONG** — they generalised from a single match:
  1. **THE ORDER IS ASCENDING JERSEY NUMBER (91.5% of 446 lineups), NOT ROTATION ORDER.** Tested via the structural signature of a 5-1 (players in slots *i* and *i+3* are opposite, so the two MBs sit exactly 3 apart, likewise the two OHs and S/OPP — a test invariant to where the listing starts). Measured **MB 15.8% · OH 19.3% · S-OPP 11.2%** against a shuffled-chance null of ~20% — at or *below* chance. On the clean 5-1 subset, 16.7% on all three. **The positive control is what makes this trustworthy: the same checker scores 100.0% on synthetic true-rotation lineups.** The 8.5% non-ascending remainder was tested separately in case a minority of scoring systems emit rotation order — it does not. Rotation order is also unrecoverable indirectly: **serves are named only on aces** (a service error prints bare `Service error.`, and a serve that stays in play emits no event), and `homeScore`/`visitorScore` are `null` on most plays so rally-by-rally serve possession cannot be rebuilt either. Guarded by `scripts/test_lineups.py` (fails if the signature ever appears).
  2. **SET 1 CARRIES A REAL STARTING SIX; SETS 2+ DO NOT.** 414 of 416 set-1 lines held exactly six names. **Sets 2+ are CUMULATIVE participation lists (6–14 names)** — reading one as a lineup is simply wrong, and is the most likely way this gets re-derived badly later. Only period 1 is parsed.
  3. **The wrong-team-label rate is 2 of 409, not "half".** The previous bullet generalised from Nebraska–Pittsburgh (6482612), where Nebraska's six really is printed under Pittsburgh's `teamId` *and* name. Rare is not zero and a mis-attributed lineup is invisible downstream, so **attribution is still by matching names to that game's box score**, never the feed's label (R8). Disagreements are recorded, so the rate stays measurable. Also corrected: the separator is `;` in some games and `,` in others.
  4. **⚠ CORRECTED SAME DAY — 2026 IS *PARTLY* POPULATED, NOT EMPTY.** The earlier claim ("2026 returns the scaffold with 0 names filled") generalised from **one match**, 6639844 (Louisville–Texas A&M), which really is blank. Crawling all three finals of 2026-08-21/22 gives **2 of 3 games with real lineups** — e.g. Kentucky (`teamId 45920`): Trinity Ward L/DS #1 · Kassie O'Brien S #6 · Kennedy Washington MB #10 · Lizzie Carr MB #15 · Brooklyn DeLeye OH #17 · Asia Thigpen OH #20. So **live 2026 lineups ARE collectable**, per match, and `crawl_pbp.py` now runs **daily** in `.github/workflows/daily.yml` to take them as they happen (the same "may not be re-crawlable later" reasoning as the player lines). `starters_lines_with_names` in each record is the counter that says whether a match carried them. **The jersey-order finding holds in 2026:** all four 2026 lineups are in ascending jersey number, so rotation order is still absent. **Lesson repeated: do not characterise a feed from a single game** — that is the same mistake as the "half the lines are mislabelled" claim, made twice in one day.
  5. **"WHO SUBS FOR WHO" IS ALSO NOT RECOVERABLE — 4%, measured.** The feed records only the player coming **IN**, never who went out (99.5% of `subs:` lines carry exactly one name). The volleyball rule that a sub may swap with only one specific starter makes the pairing deterministic in exactly one case — one non-starter on court and a starter enters. Over 206 games: **439 of 11,074 entries (4.0%) resolve; 3,608 starter-returns stay ambiguous.** The ambiguity is structural (several subs off court at once, and most entries are the libero rotating in and out), not a parsing gap. **Not built.** Nor is **setter front-row vs back-row**, which IS the rotation order. Libero-in-the-listed-six varies match to match for 171 of 279 teams so it is partly real, but 93 never show one and 15 always do — a scorer convention, not tactics. Left out.
  6. **WHAT THE STARTING SIX DOES SUPPORT: the offence, 5-1 vs 6-2.** One setter in the six is a 5-1, two is a 6-2. **253 teams consistently start one setter, 4 start two, 81 not stated.** Badged only when a team's own lineups agree ≥80%; a team whose position data cannot show a setter gets **no label rather than a guess** (6 teams show zero setters — that is missing data, not a system). Hand-verified: Missouri St. and Grand Canyon genuinely start two setters. `scripts/project_lineups.py:offense_system()`, guarded in `test_lineups.py`.
  **Storage:** raw PBP is ~296 MB/season, so `scripts/crawl_pbp.py` extracts and discards, writing compact `data/raw/{season}/lineups.jsonl`. `--keep-raw` retains payloads locally.

- **⚠ NCAA.COM'S ATHLETICS-SITE URLS ARE STALE, AND THAT — NOT THE PARSER — WAS THE ROSTER GAP (fixed 2026-08-22).** 30 teams had no 2026 roster. `athletics_sites.json` mirrors ncaa.com's school pages, and **ncaa.com itself carries dead domains**: it still lists `suathletics.com` for Syracuse (really `cuse.com`), `buffalobulls.com` for Buffalo (`ubbulls.com`), `wakeforestsports.com` (`godeacs.com`), `gocolgateraiders.com` (`colgateathletics.com`), `goseawolves.org` for Stony Brook (`stonybrookathletics.com`), plus ten more; two entries were malformed with a **doubled scheme** (`https://https://…`). Corrections live in `data/raw/2026/athletics_sites_overrides.json` — a separate file, so the mirrored one stays a faithful record of what ncaa.com says. **346 of 348 teams now have a full roster** (was 320). Only Central Conn. St. and Tennessee Tech remain.
  **A domain is never accepted because it resolves.** `recover_missing_rosters.py` requires one of two independent confirmations and records which fired: **(a)** the recovered roster shares ≥2 surnames with that team's own 2025 box scores, or **(b)** the site names the school. Both are needed because either alone fails: a parked domain would pass nothing, but **Syracuse returned only 1 of 16 players and was wrongly rejected at a ">=2" cutoff I invented** — a verdict resting on a threshold I chose has tested nothing (R1). Check (b) cleared it; the weak overlap is stored, not hidden.
  Three parser gaps fixed along the way, all of which made a roster read as **empty** rather than error: the jersey number wrapped **inside** the player's own link (`<span>#1</span> Hailee Mack` → fails the name-shape test); pages that render the roster in **JavaScript** (hrefs are literal JS expressions) but still ship the squad as **schema.org `Person` blocks** — names only, so class and position stay unlisted rather than invented; and short sport codes (`/sports/vb/roster` at LSU), tried **last** because a bare `vb` may be the men's roster elsewhere — which the overlap check would catch.
  **`New Orleans` was never a crawl failure at all** — the hub says `New Orleans`, the roster file says `"LSU New Orleans "` **with a trailing space**. Joining on team NAMES again. Fixed by reusing `reconcile_2025.norm()` rather than writing a second normaliser.

- **TRANSFERS RECONCILE IN BOTH DIRECTIONS — USE IT AS A CHECK (built 2026-08-22).** A transfer is the one player who appears on two teams' pages at once, so the two records must agree, and that makes a free correctness test. Her old team lists her under departures with a season point total; her new team shows a rate and set count taken from that same season **at the old school** (looked up in `players_2025.json`, anchored on `(from_team_id, name)` — **555 of 562 resolve**; a bare-name lookup over 6,017 players is the wrong-person match R8 exists for). **rate × sets must equal departure points: 185 of 185 reconcile, 0 mismatches.** Guarded in `test_display_invariants.py`. Also derived: **where a departed player WENT** — inverting the official transfer map identifies **191 transfers out** among teams' top-3 departures, so a graduating senior is no longer lumped in with a player who walked to another programme (Suli Davis BYU→SMU 591.5 pts, Morgan Gaerte Notre Dame→Kentucky 575.0). Production is computed from **raw counts**, never the box score's `points` column.

- **⚠ A STICKY HEADER CAN HIDE ROW 1, AND THE DATA WILL LOOK WRONG (paid for 2026-08-22).** Cody: *"nebraska fell off the rankings lol. there is no #1."* Nothing was wrong with the model, the rating or the payload — Nebraska was rendered at row 1 the whole time with **its own column header parked on top of it**. Cause: the nav was made `position:sticky`, and table headers were offset by `top:var(--navh)` to clear it — but the rankings and leaders tables stick inside **their own `overflow:auto` box**, where that offset pushes the header 42px **down**, over the first row. Fix is `.scroll th{top:0}`; only page-level sticky headers need the nav offset. **Lesson: a sticky offset is relative to the nearest scroll container, not the page** — check which one each table has before offsetting globally. Guarded in `test_display_invariants.py` (fails if the page-level offset exists without the scroll-box cancel), verified to catch the reintroduced bug. Tab switching now also returns to the top, because a sticky nav lets you change tabs from deep in a long page and the old scroll position carried over. **This is R5's cousin: the number was right and the page still lied.** A DOM/geometry assertion caught nothing here — `elementFromPoint` at the row's centre is what proves a row is actually painted.

- **ROSTER POSITIONS: 81% AND THE REST IS A JS CEILING (measured 2026-08-22).** School roster pages gave a position for only **39.5%** of players — `crawl_rosters.py` matches short codes (`OH|MB|OPP|RS|DS|L|S`) in the 300 chars after the name anchor and misses sites that spell out "Outside Hitter". Three sources now stack, best first: the school's own listing → a **second pass** (`scripts/crawl_roster_positions.py`, spelled-out titles first, short codes as fallback, writes its **own** file and never rewrites `rosters_2026.json`, so it cannot drop a player or regress the R8 join) → last season's box score → for a **transfer**, her previous school's box score anchored on `from_team_id` (**552 of 562 resolve**; a bare-name lookup across 6,017 players would invite the wrong-person match). Result: **81.2%**. The rest is a genuine ceiling — most roster templates are **Knockout/SIDEARM rendering the table in JavaScript**, so the HTML carries the "Pos." header and no data rows (same ceiling as the photos). Missing is mostly **true freshmen (654)**, who have no box score either. ⚠ **A 15-team sample estimated the yield at 1-in-15; the real rate was 95 of 234. Do not size this kind of crawl off a small sample.** ⚠ **`O` IS NOT "OPPOSITE" — never map it.** Of the 41 box-score `O` players who also carry a school-site position: **27 OPP, 8 OH, 5 S, 1 RS** — wrong about a third of the time. It renders as "Position not listed". ⚠ **NEVER backfill a 2026 jersey number from 2025** — players change numbers, and roster vs box-score number already agreed 0 of 6 times. Live 2026 box scores only.

- **PHOTOS: 133 → 290 TEAMS, VIA THE SAME JSON-LD ROUTE (2026-08-23).** Photo coverage was stuck at **38.4% of players / 133 of 347 teams** because `crawl_rosters.py` looks for an `<img>` near the player's anchor, and most roster pages build their table in JavaScript. Those same pages still emit the squad as schema.org `Person` blocks **carrying `image.url`**. `scripts/crawl_roster_photos.py` reads them: **196 teams gained photos, 3,240 URLs**, taking the projected-six to **89.9% (1,692 of 1,883 slots)** and **290 of 348 teams** with at least one. Two URL repairs carried over and verified live (14/14 sampled return HTTP 200): WMT's **doubled prefix** (`site.com/https://site.com/imgproxy/…`) 404s, and SIDEARM crop URLs need `&amp;` decoded. **URLS ONLY — never downloaded, never committed**; a `data:` placeholder is discarded, and a player without a photo renders **initials**. Guarded in `test_display_invariants.py` (every photo value must be a remote URL, and the initials fallback must still exist). ⚠ The same JSON-LD route also unlocked **rosters** for JS-rendered pages — see the athletics-URL entry below.

- **⚠ HIDING THIRD-PARTY DATA IS NOT THE SAME AS NOT PUBLISHING IT (caught 2026-08-23).** The first public build removed the VolleyTalk and Massey **columns** and passed a guard that searched for the strings "VolleyTalk" and "Massey Ratings". It was still shipping **25 VolleyTalk ranks and 151 Massey ranks inside `const TEAMS`** — invisible on the page, one devtools open away on a PUBLIC site. The columns were cosmetic; the payload was the publication. Fixed by dropping the values at the single point they enter the build (`BOARD.build()`), so no downstream consumer can reintroduce them. **The guard now asserts the VALUES, not the words.** General lesson for this project: when the question is "did we publish X", grep the DATA, not the markup.

- **RESOLVED 2026-08-23 — THE AQ COUNT IS 32 FOR 2026, AND 31/33 WAS NEVER A FALLBACK.** The open note read "AQ config 32/32 with 31/33 fallback", as if one were a guess. They are **different seasons**. NCAA.com's own 2025 field announcement: *"Thirty-one conferences were awarded an automatic qualification, while the remaining 33 positions were filled with at-large selections."* Our 2025 data agrees exactly — 31 conferences, all 31 represented in `actual_field_2025.json`. **The difference is the Pac-12**, which fielded no D-I volleyball league in 2025 and returns for 2026 with **9 members** (Boise St., Colorado St., Fresno St., Gonzaga, Oregon St., San Diego St., Texas St., Utah St., Washington St.). So 2025 = 31 AQ + 33 at-large, 2026 = **32 AQ + 32 at-large**. ⚠ **There are 33 conference LABELS in 2026, not 32** — the extra is the **WAC, which has exactly one D-I member left** (UT Arlington, still served under `wac` by a league that no longer fields a volleyball championship). Verified the bracket handles it: 32 AQs awarded, **WAC 0**, Pac-12 1, no conference double-bid. **A conference label is not a conference** — count members before awarding anything.
  **Still open: the AQ MECHANISM (tournament vs regular-season champion) is confirmed for only 6 of 32.** ⚠ **It cannot be derived from our own data, and the attempt is documented so it is not retried:** a conference tournament and a late regular-season finale are indistinguishable in the game log, exactly as `crawl_aq.py` says (the feed carries no bracket structure). Counting late intra-conference games fails — the ACC shows 36 and is a *confirmed regular-season* league; ending in a single game fails too. The one thing the data DOES show correctly is the Big Ten not ending 2025 in a title match, consistent with its tournament being new for 2026. The remaining route is per-conference external evidence, 26 lookups. No consolidated source exists (searched).

- **⚠ THE RANKINGS TAB WAS A PRESEASON PROJECTION, NOT A RANKING (found + fixed 2026-08-23).** Cody asked whether it updates weekly or constantly. **It did neither.** `rank26` came from `projection_2026.json` — 2026 rosters × 2025 production — which reads **no 2026 result at all**, so it could not move when a team won or lost. Proof: Texas lost 1-3 to Arizona St. and stayed **#2**; ASU stayed **#8**. It was rebuilt nightly, which made it *look* live. Correct for what it IS (a projection); wrong for what a tab called "Rankings" is read as.
  **Now:** `build_rankings_board.py` prefers `data/rating_{SEASON}.json` — the in-season composite (RPI + opponent-adjusted net points/set, weights fitted on 2025) — and falls back to the projection only while one does not exist. `rating_2025.py` is season-parameterised and **refuses to fit under 50 played matches**, so the switch happens **automatically** once the season has a schedule graph worth rating (4 D-I matches as of this writing). The page states which basis it is on, in those words, so the heading cannot imply more than the number knows.
  **Weekly archive:** `scripts/snapshot_rankings.py` freezes the ranking **Mondays only** (the job runs 09:15 UTC = 05:15 ET, genuinely pre-week), **one row per ISO week**, **append-only** — a past week is never rewritten, because re-deriving it later from data that includes that week's results would be a fit, not a ranking. `data/rankings_history_*.jsonl` is committed by the daily workflow; it is the one artifact here that **cannot be rebuilt**. Each row records `source` (live vs preseason) — the two are different claims and the archive must not blur them.
  **Movement** (▲/▼) compares against the last snapshot that is not this week's **and is on the SAME BASIS**. ⚠ **Caught by review, 2026-08-23 — the first version compared by date alone and broke the invariant written one line above it.** On the crossover Monday, when the season passes 50 matches and the live composite replaces the projection, every earlier row is preseason while the current rank is live: subtracting them is arithmetic on two different rulers. A mid-major at #14 on a roster projection sits near #80 on a rating that punishes weak schedules, so the page would report a confident **▼66** for a team that did nothing. **The ruler moved, not the team.** Blank is the correct output; `pick_comparison()` returns None and `mover()` renders "". Guarded in `test_rankings_history.py` with a negative control (the test fails against the old date-only filter). **The original synthetic-week test passed because both its rows happened to be preseason** — a test can be green and still not exercise the case that matters. ⚠ **Blank and "–" mean different things and must stay distinct:** a dash means we compared and it did not move; blank means there is no earlier week to compare against. Guarded in `scripts/test_rankings_history.py`, and verified end-to-end against a synthetic prior week (Texas 8→2 renders ▲6, ASU 2→8 renders ▼6) rather than waiting a week to find out.

- **ONE BUILDER, TWO PAGES — THE PUBLIC DASHBOARD IS NOW THE HUB (2026-08-23).** `scripts/build_hub.py` builds Cody's private page; **`build_hub.py --public`** builds `output/vb_dashboard.html`, the page GitHub Pages serves. They were two different products, which is how the public one ended up years behind and feeling like a different site. **`build_vb.py` is superseded and no longer runs in the daily workflow** (kept on disk as the record of the old 3-tab template).
  **The public build strips every source that is not ours to republish** — the VolleyTalk Top 25, Massey Ratings and the forum-transcribed TV listings — done as a post-pass (`strip_private()`) so the transformation is one readable place, then **ASSERTED: the build ABORTS if a marker survives**. Venue/event names stay, because `data/venues_2026.json` is already tracked. ⚠ **A marker must not collide with data:** a bare `"Massey"` tripped on **Addison Massey and Alexis Massey**, real players on real rosters — match the product (`"Massey Ratings"`) and the markup (`data-v="tv"`), never a word that can be somebody's surname. Guarded in `test_display_invariants.py`, which also checks the rankings header and cells still align after three columns are removed.
  `--public` also rewrites `index.html` with a content hash (inherited from `build_vb.py`): Fastly holds the object ~10 minutes, so without a new URL a deployed fix keeps serving old bytes long enough for a phone check to call it broken. **`index.html` is committed by the daily workflow** for the same reason.
  **Verified CI-safe:** the public build was run with `Cody/` moved aside (CI checks out a tree without it, since it is gitignored) — `load_pipe()` returns `[]` for the missing VT/Massey files, so it completes cleanly.
  **UI fix:** the Teams tab opened **completely blank** — a lone "Type a team" box with no hint of what lived there. It now lands on the top-ranked team and carries a hint line; an existing selection is never overwritten.

## What exists here
`output/vb_dashboard.html` (built dashboard, **40-team sample**, embedded logos) + `vb_template.html` (source; `scripts/build_vb.py` injects data). `data/vb_*.json` = the 40-team datasets (model, rosters, transfers, official RPI, logos). Scale to **all 348** in Phase 1.

## Git
Repo initialized 2026-08-09 on `main`, commit `499a537` (20 files). Identity set **repo-local** (`Cody` / GitHub noreply) — global config untouched. `.gitignore` blocks credentials preventively (there are none: Drive auth is the claude.ai MCP connector's server-side OAuth, not a local `token.json`), plus `.claude/settings.local.json` and `full terminal convo.pdf` (transcript, not a build input — one line to re-include). `gh` authed as `6vrhm29rzs-a11y`.
**Pushed 2026-08-09 ~23:50** — live and public at `https://github.com/6vrhm29rzs-a11y/wvb-hub`, remote `main` = `a240483`.
**Verify remote state with `git ls-remote origin` on boot** — do NOT trust the last written statement about it. (A 2345 session-close said "PUSH NOT LANDED"; the correcting amendment was filed as a *separate* Drive doc and went unread on the next boot. Corrections go in the doc they correct, or the superseded doc carries a pointer.)

## Next steps

**⚑ 2026-08-22 — WHERE THIS ACTUALLY IS.** The season is live (first matches 8/21). Everything below the "Phases 1-3" heading is historical; read this block first.

**Green and verified this session:** all 7 test suites pass · sim, predictions and hub all rebuild clean · churn wired (above) · `Cody/START-HERE.html` is the single page Cody opens (2.3 MB, tabs: scores · rankings · teams · leaders · players · standings · bracket · schedule · tv).

**RESOLVED 2026-08-22 — rotations and starting positions (Cody's newest ask).** Feasibility ran to a conclusion; see `docs/rotations_finding.md` and the ROTATIONS bullet above.
- **Rotations 1-6: NOT POSSIBLE from this feed, and the question is closed.** The order is ascending jersey number (91.5%), not rotation order — measured at chance against a positive control that scores 100%. Serves are named only on aces, so the serve sequence (which *is* rotation order) cannot be recovered either. Told Cody plainly rather than shipping a plausible-looking guess.
- **Starting lineups: BUILT.** `scripts/crawl_pbp.py` (extract-and-discard, 296 MB/season not committed) → `data/raw/2025/lineups.jsonl`; `scripts/project_lineups.py` → `data/lineups_2026.json` = each team's most-started six in 2025 with each player marked returning/departed/unknown for 2026. **Vacated slots render as VACANCIES, never a guessed name** (R5). Guarded by `scripts/test_lineups.py` (8 groups incl. positive control + a negative control on the "0 returning vs no roster" artifact).
- **Still open here:** the full-season crawl finishes ~1.8 h after launch (43 games/min, resumable — just re-run `python3 scripts/crawl_pbp.py`); re-run `project_lineups.py` afterwards. **NOT yet wired into the hub page** — `build_hub.py` does not read `lineups_2026.json` yet.

**OPEN — carried:** photos beyond the 132 teams covered (215 have JS-loaded roster templates — a genuine ceiling, not a bug) · AQ mechanism confirmed for only 6 of 32 conferences · **Cody has not yet run `/code-review ultra`** (Builder cannot launch it).

**Known-honest caveats that must keep being said out loud on the page:** the roster aggregation **never beats the prior alone** out of sample (0.806 vs 0.827) — it is a *correction* to last season's composite, not a foundation. Texas is #2 largely because it was #2 last year. Say that rather than letting the number imply more than it knows.


**2026-08-11 — THE 2026 TAB IS WIRED AND THE JOIN QUESTION IS CLOSED.** Returning production runs on the real join (2026 school rosters × 2025 per-player production) for **309 of 348** D-I teams; the other 39 render **—**, nothing estimated. Verified: distribution smooth (median 0.570, no hash-style cluster), Nebraska reconciles by hand (1455.5/2074.0 = 0.702), display invariants + provenance both green. The method sentence on the page is now selected by `returning_method`, so the number and its label cannot drift apart (R4).
**Fixed getting here:** Miami (FL)'s 18 unresolved were a WMT parser bug — the headshot anchor reads `"<Player Name> Photo"`, so every player survived twice (30 "players" for a 15-player roster). Parser fixed; the crawled file repaired in place by `scripts/repair_rosters_2026.py` (idempotent, no re-crawl). Miami 18 → 3.
**NEXT ACTION: run `scripts/crawl_aq.py`** — the conference AQ map. Written, syntax-tested, **never run**. ~32 conference sites. Six rows are already CONFIRMED by Claude-app and are used as given, never re-derived.
**Then:** ask Cody to run `/code-review ultra` (evidence-backed, see R7), and confirm the daily pipeline fires on its **cron** rather than a manual dispatch — still the last unverified claim about the whole thing running itself.

**THE BUILD IS COMPLETE (2026-08-10).** Pipeline autonomous · RPI reconciled 348/348 · fitted composite beats RPI out-of-sample · field projector 62/64 · rally simulator calibrated (Brier 0.1289, every bucket within 3.4 pts over 5,014 matches). **Next action: none.** Let it run a week against live data and see what breaks in reality rather than in a dry run.
**Do NOT start without Cody deciding:** KPI scraping (faktorsports), the momentum term (series length under-predicts sweeps 4.6 pts — rally-independence signature, reported not tuned), a conference-bracket data source (the two AQ misses), the Sheets override layer.

**Phases 1-3 detail.** Rating shipped: composite = 1.27·Z(RPI) + 1.27·Z(opponent-adjusted net points/set), weights **fitted** not hand-entered, beats RPI out-of-sample at three cutoffs (+0.025/+0.019/+0.016 AUC, CIs clear of zero). Dashboard runs on all 348 with the vs-RPI delta column. **⚠ It is a STRENGTH rating, not a RESUME rating** — relative to RPI it favours teams with *worse* records (corr −0.205), so it is not automatically right for at-large bracketology. Cody's call.
**✅ The two live-crawl bugs are FIXED and TESTED** (see R2 and `scripts/test_crawl_freshness.py`, verified to fail against the old behaviour). Run that test before any live-season crawl.
**Measured:** the opponent adjustment survives tiny samples (ridge holds the range to ±11.6 at *three* games/team) and its edge over RPI is **largest in September** (+0.064 AUC at 6 games/team, decaying to +0.016 by November) — RPI needs a schedule graph before it means anything, margin does not.

**Phase 1 detail (2026-08-10).** `data/data_2025.json` = 384 teams (348 D-I + 36 non-D-I opponents), 5,131 games, raw counts + linescores, source-tiered, dated. Snapshot in `data/snapshots/`. Raw crawl in `data/raw/2025/`. Scripts: `crawl_2025.py` (resumable), `reconcile_2025.py`, `build_dataset.py`.

**Phase 2 — build RPI Factors I–III** (the spec now exists: Research/ "RPI Spec, Selection Criteria…" = `1VtM8pdKOtz6RazClpYChKt6ozrp5fFeMCQBzXswgo0U`). **25% W% / 50% OW% / 25% OOW%, UNWEIGHTED — no home/road multipliers** (volleyball adopted neither basketball's 0.6/1.4 nor baseball's 0.7/1.3; blogs claiming otherwise are wrong). D-I opponents only; self-exclusion in Factors II/III is INFERRED convention — log it as such. Factor IV is expressed in approximate RPI *positions* and is NOT exactly reproducible — label any overlay DERIVED. Reconcile vs the published rank ordering; divergence concentrated on teams with marquee wins / bad losses is **expected and is evidence the base calc is right**.

**Then the metric bake-off** — still deferred by design: net points/set vs TCV vs original Adj vs hitting-efficiency differential, measured against 2025 outcomes. Claude-app pre-registered its expectation (hitting-eff differential and net pts/set lead) — keep that falsifiable.

**Open / carry forward:** snapshot growth (4.3 MB per run — daily in-season is ~500 MB/season, needs a retention policy); AQ config 32/32 with 31/33 fallback; Big Ten + Pac-12 now award AQ by **tournament** (new for 2026); KPI is read-only from faktorsports.com (THIRD-PARTY tier).
